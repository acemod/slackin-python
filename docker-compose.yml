version: '2'
services:
    slackin-python:
        build: .
        ports:
            - "80:80"
        environment:
        - SLACKIN_INTERVAL=30000
        - SLACKIN_SUBDOMAIN=
        - SLACKIN_APITOKEN=
